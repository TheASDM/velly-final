[tool.pytest.ini_options]
testpaths = ["chatbot/tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py39"
line-length = 100
exclude = [
  ".git",
  ".eleventy.cache",
  "_site",
  "backups",
  "node_modules",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
